# Netlify redirects file for SPA routing
# This file will be copied to dist/public during build

# Redirect all routes to index.html for client-side routing
/*    /index.html   200

# API proxy - uncomment and update if backend is on separate domain
# /api/*  https://YOUR_BACKEND_URL/api/:splat  200

