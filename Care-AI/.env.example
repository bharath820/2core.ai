# Database Configuration (SQLite)
# Leave empty to use default: health_wallet.db in project root
# DATABASE_URL=./health_wallet.db

# Session Secret (change in production!)
SESSION_SECRET=health_wallet_secret_key_change_in_production

# Server Port
PORT=5000

# Node Environment
NODE_ENV=development
